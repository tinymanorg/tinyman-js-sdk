import {encodeString} from "../util/util";

export const MINT_APP_ARGUMENT = encodeString("mint");
export const MINT_FLEXIBLE_MODE_APP_ARGUMENT = encodeString("flexible");
export const MINT_SINGLE_MODE_APP_ARGUMENT = encodeString("single");
